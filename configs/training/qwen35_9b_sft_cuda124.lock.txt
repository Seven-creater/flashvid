# Reproducible Qwen3.5-9B LoRA training stack for Python 3.12.
# PyTorch is installed first from SHA-pinned Aliyun domestic copies of the
# official cu124 wheels by scripts/install_ms_swift_442.sh. PyTorch 2.6 pins
# Triton 3.2, which flash-linear-attention 0.4.2 requires at import time.
torch==2.6.0+cu124
torchvision==0.21.0+cu124
torchaudio==2.6.0+cu124
triton==3.2.0

# ms-swift 4.4.2 and the dependencies required by its Qwen3.5 guide.
ms-swift==4.4.2
transformers==5.9.0
qwen-vl-utils==0.0.14
peft==0.19.1
datasets==4.8.4
accelerate==1.14.0
trl==0.29.1

# Qwen3.5 hybrid/linear-attention fast path.  Version 0.4.2 publishes a
# platform-independent wheel (and depends on the platform-independent
# fla-core wheel), so the installer can reject all source builds.
ninja==1.13.0
packaging==25.0
flash-linear-attention==0.4.2
fla-core==0.4.2

# transformers==5.9.0 provides an official torch Conv1d fallback when
# causal-conv1d is absent and SDPA for full-attention layers.  Do not add
# causal-conv1d or flash-attn here: PyPI only publishes source archives for
# the pinned versions, which would require an unavailable CUDA build toolkit.
